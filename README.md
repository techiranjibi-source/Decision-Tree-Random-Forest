# Decision-Tree-Random-Forest
This project demonstrates the implementation of Decision Tree and Random Forest algorithms using Scikit-learn. It includes model training, overfitting control using max depth, feature importance analysis, and performance evaluation using cross-validation on a classification dataset.
